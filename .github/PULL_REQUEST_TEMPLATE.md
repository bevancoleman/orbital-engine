## What this changes and why

## Checklist

- [ ] `npm run typecheck && npm run lint && npm test && npm run build` pass
- [ ] `npx playwright test` passes, if this touches rendering behavior
- [ ] Added a changeset (`npx changeset`) if this needs a release note, or this is docs/CI-only and doesn't need one
