---
"orbital-engine": minor
---

Add optional `wikiSummary`/`wikiUrl` fields to `CelestialBody` and `BeltRegion`, rendered in the selected-body/belt info panel below the existing provenance `note` when present. Both fields are consumer-supplied free text/links — the engine has no opinion on where they came from.
